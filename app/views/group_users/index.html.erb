<h1>All Followers</h1>

<table class='table'>
  <thead>
    <tr>
      <th>login</th>
      <th>relation</th>
      <th>function</th>
    </tr>
  </thead>
  <tbody>
    <% @followers.each do |follower| -%> 
      <tr>
        <td><%= follower.login %></td>
        <td><%= follower.position(@group) %></td>
        <td>
          <%= link_to("prove", prove_group_user_path(@group, follower)) if @group.unprove_users.include?(follower) %>
          <%= link_to("remove", group_user_path(@group, follower), :method => :delete) unless @group.owners.include?(follower) %>
        </td>
      </tr>
    <% end -%>
  </tbody>
</table>
